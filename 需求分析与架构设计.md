# 文本大数据知识抽取与图谱构建工具 — 需求分析与架构设计

## 一、项目背景

基于《文本大数据知识抽取与图谱构建工具操作手册》，采用 AI 开发（Vibe Coding）模式，复刻并增强一套完整的知识图谱构建平台。系统覆盖从数据接入、知识抽取、图谱构建到图谱探索的全链路能力，并在操作手册基础上引入大模型技术提升抽取效果与交互体验。

### 1.1 基本要求

| 约束项 | 要求 |
|--------|------|
| 后端语言 | Java + Python 混合（Java: Spring Boot 3.5 / Python: FastAPI 微服务） |
| 前端框架 | Vue 3 |
| 数据库 | MySQL（业务数据） + Neo4j（图谱数据） |
| 大模型 | 使用线上免费 API（现有 DeepSeek API） |

### 1.2 扩展要求

1. 在操作手册界面基础上，设计更友好的 UI 及交互体验
2. 功能不受限于手册，可根据知识图谱系统理解合理增改
3. 合理引入大模型技术（不考虑速度和性能，可使用免费 API）

---

## 二、功能需求分析

### 2.1 功能全景

系统分为 **5 大一级模块**，共 **17 个功能页面**：

```
文本大数据知识抽取与图谱构建工具
├── 1. 用户管理模块
├── 2. 知识图谱管理
│   ├── 2.1 新建项目
│   ├── 2.2 编辑项目
│   ├── 2.3 存储引擎配置（预留）
│   └── 2.4 导出配置文件（预留）
├── 3. 图谱模型管理
│   ├── 3.1 新建模型
│   ├── 3.2 清空模型
│   ├── 3.3 复制模型
│   ├── 3.4 编辑模型
│   ├── 3.5 对象及属性管理（实体类型 + 属性 CRUD）
│   ├── 3.6 关系管理（关系类型 + 属性 CRUD）
│   └── 3.7 可视化
├── 4. 知识抽取与转化
│   ├── 4.1 （半）结构化数据转化 ✅ 已实现
│   ├── 4.2 基于 KOS 知识抽取 ✅ 已实现
│   ├── 4.3 基于深度学习知识抽取 ✅ 已实现（模拟训练 + 词典匹配推理）
│   ├── 4.4 基于 LLM 知识抽取与转化 ★（核心）✅ 已实现
│   │   ├── 4.4.1 语料管理（含 MinerU 文档解析）✅ 已实现
│   │   ├── 4.4.2 AI 模型配置 ✅ 已实现
│   │   ├── 4.4.3 抽取执行与写入 ✅ 已实现
│   │   └── 4.4.4 文本切分（防止上下文窗口超限）🔧 规划中
│   └── 4.5 智能问答（LangGraph Agent · SSE 流式）✅ 已实现
└── 5. 图谱实例管理 ✅ 已实现（图谱探索）
```

**业务流程**：用户登录 → 创建图谱项目 → 创建图谱模型（定义实体/关系类型） → 知识抽取时选择模型 + 语料 → LLM 抽取 → Python 直接写入 Neo4j（按 modelId 隔离）→ 图谱探索查询

### 2.2 功能模块详细需求

#### 2.1 系统首页

**功能描述**：平台概览仪表盘，展示核心运行指标与快捷入口。

**功能点**：
- 统计卡片：图谱项目数、实体总数、关系总数、抽取任务数
- 图表：近 7 天抽取趋势（折线图）、实体类型分布（饼图）
- 快捷操作入口：新建图谱、上传语料、LLM 抽取、图谱探索

#### 2.2 平台管理

##### 2.2.1 用户管理

**功能描述**：管理系统用户账号。

**功能点**：
- 用户列表（分页、搜索、按角色筛选）
- 新增用户（账号、昵称、头像、角色、简介）
- 编辑用户信息
- 删除/禁用用户（逻辑删除）
- 重置密码

##### 2.2.2 角色管理

**功能描述**：基于 RBAC 的角色与权限管理。

**功能点**：
- 角色列表（管理员、标注员、普通用户等）
- 角色增删改查
- 角色分配菜单/功能权限（权限树）
- 用户-角色绑定

##### 2.2.3 修改密码

**功能描述**：当前登录用户修改自身密码。

**功能点**：
- 输入旧密码、新密码、确认新密码
- 密码强度校验
- MD5 + 盐值加密存储（沿用现有方案）

#### 2.3 图谱项目管理

##### 2.3.1 知识图谱管理

**功能描述**：管理知识图谱项目，一个项目可包含多个图谱模型。

**功能点**：
- 新建项目：输入项目名称、项目负责人、项目描述、选择存储引擎（目前仅 Neo4j）
- 编辑项目：修改项目名称、项目负责人、项目描述
- 项目列表：分页查询、按名称搜索
- 删除项目：逻辑删除
- 存储引擎配置（预留）：配置 Neo4j 连接信息，当前使用全局配置
- 导出配置文件（预留）：导出项目信息 + 模型定义
- 项目详情：展示项目下的模型列表、实体/关系统计

##### 2.3.2 图谱模型管理

**功能描述**：定义图谱的本体模型（Schema），包括实体类型、关系类型及其属性。一个项目可创建多个模型，抽取时通过 modelId 隔离图谱实例。

**功能点**：
- 新建模型：输入模型名称、模型版本、模型描述
- 编辑模型：修改模型名称、版本、描述
- 清空模型：清空该模型下所有实体类型和关系类型（保留模型本身，二次确认）
- 复制模型：以"模型名称-副本"创建副本，复制所有实体类型、属性、关系类型
- 对象及属性管理：
  - 实体类型 CRUD：新增实体名称、描述、颜色、图标
  - 实体属性 CRUD：为每种实体类型添加/删除属性（属性名、数据类型、是否必填）
- 关系管理：
  - 关系类型 CRUD：新增关系名称、描述、起点实体类型、终点实体类型
  - 关系属性 CRUD：为每种关系类型添加/删除属性
- 可视化：以图形方式展示实体-关系模型（节点 + 连线，类似 Neo4j）
- **增强**：支持 LLM 辅助生成本体模型 — 输入领域描述，自动推荐实体和关系类型

#### 2.4 知识抽取与转化

**模块说明**：知识抽取模块提供四种互补的抽取方式，统一通过 `extraction_task` 表记录任务（`extractionType` 字段区分：STRUCTURE/KOS/DL/LLM），抽取结果均按 `modelId` 隔离写入 Neo4j。**四种抽取方式均已实现**：结构化抽取（Java 直写 Cypher）、KOS 抽取（Python TF-IDF + 三层词表）、深度学习抽取（模拟训练 + 词典匹配）、LLM 抽取（Prompt + LLM API）。语料管理模块集成 **MinerU 文档解析**，支持 PDF/Word 文件上传后异步解析为 Markdown 文本入库；规划中的文本切分功能将在 MinerU 解析完成后将长文本切片存储，防止 LLM 抽取时上下文窗口超限。

##### 2.4.1 （半）结构化数据转化

**功能描述**：将 CSV、Excel、关系数据库表等（半）结构化数据通过字段映射转化为图谱实例（节点与关系），适用于已有结构化数据资产的快速入库场景，无需 LLM 调用，转化效率高。

**设计思路**：以"字段映射 + 类型转换"为核心，用户上传结构化文件或指定数据库表，系统根据所选图谱模型的本体 Schema（实体类型、关系类型及其属性）提供映射配置界面，将源字段映射到目标实体/关系属性，并自动处理类型转换与主键对齐，最终批量写入 Neo4j。

**功能点**：
- 数据源接入：
  - CSV/Excel 文件上传（支持拖拽、多文件）
  - 关系数据库连接配置（JDBC URL、用户名、密码、表名）—— 预留
- 字段映射配置：
  - 选择目标实体类型（来自当前图谱模型的 graph_entity_type）
  - 源字段 → 实体属性映射（下拉选择源列名 → 目标属性名）
  - 指定主键字段（用于节点 MERGE 去重，对应 Neo4j 节点唯一标识）
  - 选择目标关系类型（来自 graph_relation_type），配置源字段 → 关系属性映射
  - 关系端点映射：源表中"头实体主键列"和"尾实体主键列"分别映射到起止实体
- 类型转换规则：字符串/数值/日期/布尔自动适配 Neo4j 属性类型，非法值过滤
- 抽取执行：Java 直接读取结构化数据 → 按映射规则组装 Cypher MERGE 语句 → 批量写入 Neo4j（带 modelId 隔离），不经过 Python 微服务
- 结果展示：写入统计（成功/失败/跳过条数）、失败明细列表、预览前 10 条转化结果
- 任务记录：写入 extraction_task 表，extractionType=STRUCTURE，inputConfig 存储映射规则 JSON

**性能指标**：单文件 1 万行以内 30 秒完成转化写入；支持断点续传与失败重试。

##### 2.4.2 基于 KOS 知识抽取

**功能描述**：基于知识组织体系（Knowledge Organization System，含分类体系、叙词表、本体词表）对文本进行术语识别、概念归类与关系抽取。通过预设的 KOS 分类体系（如 PRES、CCT、CASDD、CNE、STKOS、NSTL 等）与可配置的抽取参数，从语料中识别高频术语、主题概念、范畴分类等多类型实体，并建立概念间的层级与关联关系，适用于领域文献库、政策文件等带分类体系约束的场景。

**设计思路**：以"KOS 词表驱动 + 统计算法 + 规则匹配"为核心，不依赖 LLM。用户选择语料库或输入文本，配置术语识别与概念归类的参数（数量、权重、目标分类体系、实体识别类型），Python 微服务加载对应的 KOS 词表与分类体系，通过词频统计、TF-IDF、语义关联度计算等算法识别高频术语与概念，按目标分类体系进行范畴归类，输出结构化实体与关系后写入 Neo4j。

**功能点**：

**1. 顶层：样例与输入区**
- 模式A：语料库选择（交互）
  - 用户切换至"语料库选择"时，文本域隐藏，显示下拉选择框（或模态框按钮）
  - 下拉/弹窗展示预设语料库列表（如"茶叶政策文件""农业科技新闻"等），数据绑定该语料库的 `id` 或 `path`
- 模式B：手动输入（交互）
  - 用户切换至"手动输入"时，下拉框隐藏，恢复大面积 `<textarea>` 文本域
  - 文本域默认填入测试样例文本，支持粘贴任意长文本

**2. 中层：参数设置面板**
字段分组配置如下（含类型、初始值、数据绑定逻辑）：
- 高频术语数量（input number，默认 10）
- 高频概念数量（input number，默认 10）
- 范畴分类数量（input number，默认 10）
- 分类得分依据（下拉选择框，选项：高频术语 / 语义关联等，默认第一项）
- 分类体系权重（input number，默认 1）
- 是否考虑权重（下拉选择框，选项：是 / 否，默认"是"）
- 目标分类体系（多选复选框组，选项：PRES、CCT、CASDD、CNE、STKOS、NSTL，默认全不选或按业务需要默认选中若干）
- 是否为多文档（下拉选择框，选项：是 / 否，默认"否"）
- 范畴分类前缀（文本输入框，默认空）
- 是否返回词（下拉选择框，选项：是 / 否，默认"是"）
- 实体识别类型（多选复选框组，网格或标签排列，选项：高频术语、主题概念、范畴分类、组织机构、专家学者、学术期刊、病害虫害、畜禽疾病、植物志名、动物志名、作物品种、化学物质、生猪品种等，默认全选）
  - 说明：实际实现时，实体识别类型应与当前所选图谱模型的本体 Schema（graph_entity_type）动态联动，此处列出的是 KOS 内置的领域识别能力默认集

**3. 交互与数据逻辑**
- 页面右上角（或参数面板下方）设置"开始抽取/计算"主按钮
- 点击按钮执行流程：
  1. 表单非空校验（文本域或语料库必填其一）
  2. 以 POST 请求将完整表单数据组装为 JSON payload 发送后端 API
  3. 请求期间按钮进入 `loading` 状态，表单区域显示"加载中"遮蔽层防止重复提交
  4. 请求成功后接收后端返回的图谱数据，传递给"结果可视化图谱组件"渲染

**4. 后端协作流程**
- Java 接收请求 → 加载当前图谱模型的本体 Schema → 组装 {text/corpusId, ontology, modelId, kosConfig} → HTTP 转发 Python
- Python 加载对应 KOS 词表与分类体系 → 执行术语识别、概念归类、关系构建算法 → 返回结构化 JSON（实体列表 + 关系列表）
- Python 写入 Neo4j（节点/关系带 modelId 隔离）→ 返回写入统计
- Java 保存抽取记录到 extraction_task 表，extractionType=KOS，inputConfig 存储 KOS 参数 JSON

**5. 结果展示与写入**
- 结果可视化：节点按实体识别类型着色（高频术语/主题概念/范畴分类等），关系展示概念层级与关联
- 实体列表：名称、类型、所属分类体系、得分
- 关系列表：头实体、关系类型（如"属于范畴""下位概念"）、尾实体
- 写入图谱：Python 直接写入 Neo4j，按 modelId 隔离

**性能指标**：单文档（5000 字以内）30 秒内完成抽取；多文档模式支持批量处理，单批上限 50 篇。

##### 2.4.3 基于深度学习知识抽取

**功能描述**：基于 NER（命名实体识别）+ RE（关系抽取）深度学习模型的抽取流水线，覆盖"语料标注 → 模型训练 → 抽取应用"全流程，适用于有标注数据积累、需要离线训练专用模型的场景，抽取精度高但需前期投入标注与训练成本。

**设计思路**：以"数据标注 + 模型训练 + 推理抽取"三阶段流水线为核心。标注阶段提供 BIO 标注界面，用户对语料进行实体与关系标注并导出训练集；训练阶段管理模型训练任务（基于 BERT/SPAN 等架构），记录训练指标；抽取阶段加载训练好的模型对新生成文本进行推理，输出实体与关系。整个流水线由 Java 调度，Python 执行模型训练与推理。

**功能点**：

**1. 数据标注（AnnotationAssign / DataAnnotation / AnnotationManage）**
- 标注任务分配（AnnotationAssign）：管理员创建标注任务，分配标注员、审核员，设定标注进度要求
- 数据标注界面（DataAnnotation）：
  - 文本展示区：左侧语料原文，支持句子级切分
  - 实体标注：鼠标选中文本 → 弹出实体类型选择（来自图谱模型本体 Schema）→ 生成 BIO 标注
  - 关系标注：选中头实体 → 选中尾实体 → 选择关系类型 → 生成关系三元组
  - 标注进度保存、撤销/重做
- 标注管理（AnnotationManage）：审核标注结果、驳回重标、导出标注数据集（JSON/CoNLL 格式）

**2. 模型训练（ModelTrain / ModelEffect）**
- 训练任务管理（ModelTrain）：
  - 选择训练数据集（标注导出数据）、模型架构（BERT-CRF / SPAN-BERT / BERT-RE 等）、超参数（学习率、epoch、batch size）
  - 启动训练 → Python 微服务调用训练框架 → 实时返回 loss、F1 等指标
  - 训练完成保存模型文件，记录训练版本
- 模型效果（ModelEffect）：展示训练指标曲线（precision、recall、F1）、混淆矩阵、错误样本分析

**3. 抽取应用（DlExtraction）**
- 输入区：语料库选择 / 手动输入文本（与 LLM 抽取页面一致）
- 模型选择：下拉选择已训练的 DL 模型版本
- 抽取执行：Java 接收请求 → HTTP 转发 Python → Python 加载模型推理 → 返回实体与关系 JSON
- 结果展示：原文高亮标注（实体着色）、实体列表、关系列表、置信度展示
- 写入图谱：Python 写入 Neo4j（带 modelId 隔离），任务记录 extractionType=DL

**4. 数据库支撑**
- 标注相关表（annotation_task、annotation）为深度学习抽取预留（见 5.1.3 备注）
- 模型版本表（dl_model）：记录模型架构、训练数据集、指标、文件路径、状态

**性能指标**：标注界面单句响应 < 100ms；模型训练视数据量而定（1 万句标注约 2 小时）；推理抽取单文档（5000 字）< 5 秒。

##### 2.4.4 基于 LLM 知识抽取与转化 ★（核心，已实现）

**功能描述**：利用大语言模型（DeepSeek）进行知识抽取。抽取时需选择绑定的图谱模型，通过 modelId 隔离图谱实例。Python 微服务负责 LLM 调用与抽取，Java 负责调度与记录。

**功能点**：
- 语料管理（含 MinerU 文档解析）：
  - 语料新增：支持两种输入方式
    - 文本输入（`source=manual`）：手动输入文本内容，直接入库（`status=1`）
    - 文档上传（`source=file`）：上传 PDF/DOC/DOCX 到 MinIO，异步调用 MinerU 解析为 Markdown 后回写 `content` 字段
  - MinerU 解析流程：
    - 上传 → 存入 MinIO（`bucket=kgraph`，返回 URL）→ 写入 `corpus` 表（`status=0` 处理中）→ `@Async` 调用 `MinerUService.parseCorpusAsync`
    - MinerU 调用：Java WebClient POST `{mineru.base-url}/file_parse`，multipart 上传文件二进制 + `return_md=true`，10 分钟超时
    - 响应解析：兼容 MinerU v3.4.5 格式 `{"results":{"<filename>":{"md_content":"..."}}}` 及旧版 `[{"md":"..."}]`、`{"md":"..."}` 等多种格式
    - 状态回写：成功 → `status=1`、`content=markdown`、`errorMsg=null`；失败 → `status=2`、`errorMsg` 截取前 500 字符
  - 重新解析：对 `source=file` 的语料支持重新触发 MinerU 解析（`POST /corpus/reparse`）
  - 前端轮询：列表存在 `source=file && status=0` 时，5 秒轮询刷新直至全部完成
  - 状态可视化：处理中（橙色圆点 + Loading）、失败（红色圆点 + Warning + errorMsg tooltip）、完成（可查看/编辑）
  - 语料列表：分页查看、搜索、预览内容
  - 语料关联项目，抽取时选择语料作为文本输入源
- AI 模型配置：
  - 选择 LLM 模型（DeepSeek 等）
  - 选择抽取模式：Zero-shot / Few-shot
  - Prompt 模板编辑：支持变量替换（{text}、{ontology}、{examples}）
- 抽取执行：
  - 选择图谱模型（获取实体/关系类型 Schema）+ 选择语料（或手动输入文本）
  - Java 加载模型 Schema + 语料内容 → HTTP 转发 Python
  - Python 构建 Prompt → 调用 LLM → 返回结构化 JSON
  - SSE 流式返回前端
- 抽取结果展示：
  - 原文高亮标注（实体着色）
  - 结构化展示：实体列表（名称、类型、位置）、关系列表（头实体、关系类型、尾实体）
  - JSON 原始输出查看
- 写入图谱：Python 抽取后直接写入 Neo4j（节点和关系带 modelId 隔离）
- **Prompt 模板管理**：保存常用 Prompt 模板，支持复用
- **抽取历史**：记录每次抽取的输入、输出、耗时、Token 消耗
- **增强**：支持 LLM 辅助本体对齐 — 当抽取到的实体/关系不在本体中时，LLM 自动推荐最接近的类型

##### 2.4.5 文本切分（规划中）

**功能描述**：在 MinerU 文档解析完成后，对长 Markdown 文本进行智能切分并存储切片，避免 LLM 抽取时单次输入超出大模型上下文窗口限制（如 DeepSeek 默认 64K tokens，长 PDF 解析后可达 10 万+ 字符）。当前 MinerU 解析后将整段 Markdown 直接存入 `corpus.content`，尚未做切分处理。

**设计思路**：采用三层渐进式切分策略，优先保留语义完整性。

1. **结构化切分（主策略）**：基于 Markdown 标题层级切分
   - 按 `#`、`##`、`###` 标题切分章节，每个章节作为一个独立 chunk
   - 保留章节标题作为 chunk 元信息（便于溯源定位）
   - 适用于 MinerU 输出的结构化 Markdown
2. **滑动窗口切分（兜底策略）**：对无标题或过长的章节做句子边界切分
   - 复用 `pybackend/text_processor/splitter.py` 中的 `split_into_chunks(chunk_size=500, overlap=50)`
   - 在中文句号 `。`、`！`、`？`、`\n\n` 等边界切分，保留 50 字符重叠避免语义断裂
3. **语义切分（远期扩展）**：基于 embedding 聚类切分（暂不实现）

**数据模型（规划中）**：新增 `corpus_chunk` 表存储切片

```sql
CREATE TABLE corpus_chunk (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    corpusId        BIGINT NOT NULL COMMENT '所属语料ID',
    chunkIndex      INT NOT NULL COMMENT '切片序号（从0开始）',
    totalChunks     INT NOT NULL COMMENT '总切片数',
    content         MEDIUMTEXT NOT NULL COMMENT '切片文本内容',
    title           VARCHAR(256) COMMENT '所属章节标题（Markdown结构切分时填充）',
    tokenCount      INT DEFAULT 0 COMMENT '切片Token数（预估）',
    embedding       MEDIUMTEXT COMMENT '语义向量（JSON，用于语义检索）',
    createTime      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_corpusId (corpusId),
    INDEX idx_chunkIndex (corpusId, chunkIndex)
) COMMENT '语料切片表';
```

**集成方案**：
- MinerU 解析成功后触发 `@Async` 切分任务
- 切分结果写入 `corpus_chunk` 表，`corpus.status` 在切分完成后才置为 `1`（已完成）
- LLM 抽取时：若语料存在切片，按 chunk 逐次调用 LLM，最终合并抽取结果；否则按整段文本抽取
- 前端抽取配置新增"切片模式"开关：单 chunk 抽取（默认）/ 全文抽取

**性能指标**：单文档（10 万字符内）切分耗时 < 2 秒；切分后单 chunk 字符数控制在 500-800（约 250-400 tokens），单次 LLM 调用上下文占用 < 1K tokens。

##### 2.4.6 智能问答（LangGraph Agent · SSE 流式，已实现）

**功能描述**：基于 LangGraph Agent 的对话式知识图谱问答，用户用自然语言提问，Agent 自主选择图谱工具查询并组织回答。通过 Python→Java→前端的全链路 SSE 流式推送，参考 DeepSeek 官网视觉风格实现思考过程、工具调用、最终回答的逐字流式展示。

**核心特性**：
- LangGraph Agent 编排：`astream_events(version="v2")` 订阅 5 类事件（`thinking`/`tool_call`/`tool_output`/`answer`/`done`）
- 内置 6 个图谱工具（`pybackend/core/agent_tools.py`）：
  - `search_entities(keyword, modelId, limit)` — 按名称关键词搜索实体
  - `get_entities_by_type(modelId, entity_type, limit)` — 按实体类型查询实体列表（**新增**，解决"人物有哪些"类问题递归调用）
  - `get_entity_detail(modelId, name)` — 获取实体全部属性、标签、度
  - `get_entity_relations(modelId, name, direction, relationType, limit)` — 查询实体的关系与邻居
  - `get_graph_stats(modelId)` — 返回实体数/关系数/各类型 Top 10
  - `list_entity_types(modelId)` — 列出所有实体标签及数量
- 工具调用上限约束：SYSTEM_PROMPT 明确"一次问答最多 3 次工具调用"，`recursion_limit=50` 作为兜底
- 全链路流式：
  - Python 端：`chat_agent.py` 维护 `thinking_sent_len`/`answer_sent_len` 已推送游标，切片只发送 delta 增量，再拆成约 4 字符一个 SSE 帧
  - Java 端：`ChatServiceImpl` 用 WebFlux `bodyToFlux(ServerSentEvent)` 逐事件透传，`maxInMemorySize` 禁用缓冲
  - 前端：`Chat.vue` 维护 3 条缓冲队列 + 定时器（思考/工具/回答三路独立打字机），30ms 间隔追加 DOM
- DeepSeek 风格 UI：浅色侧边栏、对话窗口无头像、用户气泡浅灰底色（`#f3f4f6`）、AI 消息纯文字布局、Markdown 渲染

#### 2.5 图谱实例管理

##### 2.5.1 知识图谱探索

**功能描述**：以可视化方式浏览、查询、分析知识图谱。

**功能点**：
- 三栏布局：左侧控制面板（模型选择/节点搜索/统计信息/操作提示）、中间 G6 画布、右侧节点详情面板
- **详情面板挤压布局**：点击节点/关系时，右侧详情面板从 flex 流中展开挤压中间图谱区域（不再是 absolute 覆盖）；关闭时 width=0 完全收回，图谱区域恢复原宽。配合 CSS 过渡动画 + G6 `changeSize` 同步实现平滑效果
- 图可视化画布：力导向布局展示节点与关系
- 交互操作：
  - 鼠标拖拽平移、滚轮缩放
  - 单击节点/关系：选中并展示详情面板
  - 双击节点：展开邻居节点
  - 点击画布空白：取消选中
- 查询功能：
  - 按实体名称搜索
  - 按实体类型筛选（统计区饼图）
- 节点详情面板：类型徽章、名称、属性列表（过滤系统字段 relationType/modelId/createTime/type/name）
- 关系详情：端点节点名称、关系类型、属性

---

## 三、扩展功能设计

在操作手册基础上，根据知识图谱系统理解和扩展要求，增加以下功能：

| 扩展功能 | 所属模块 | 说明 | 状态 |
|----------|----------|------|------|
| MinerU 文档解析 | 语料管理 | PDF/Word 上传后异步调用 MinerU 解析为 Markdown 入库 | ✅ 已实现 |
| LLM 辅助本体建模 | 图谱模型管理 | 输入领域描述，LLM 推荐实体/关系类型 | 规划中 |
| Prompt 模板管理 | LLM 知识抽取 | 可复用的抽取 Prompt 模板库 | 规划中 |
| 图谱版本管理 | 图谱项目管理 | 图谱快照，支持回滚 | 规划中 |
| 图谱数据导出 | 图谱探索 | 导出为 JSON / CSV / Cypher 脚本 | 规划中 |
| 实体消歧与合并 | 图谱实例管理 | LLM 辅助识别并合并同名/相似实体 | 规划中 |
| 文本切分 | LLM 知识抽取 | MinerU 解析后切分长文本为 chunk，防 LLM 上下文超限 | 🔧 规划中 |
| 智能问答 Agent | 智能问答 | LangGraph Agent + 6 个图谱工具 + 全链路 SSE 流式 | ✅ 已实现 |

---

## 四、技术架构设计

### 4.1 整体架构

采用 **Java 主服务 + Python 微服务** 的混合架构：Java 负责业务管理、图谱查询与 API 网关；Python 专注 LLM 密集型任务（知识抽取、实体消歧）；两端共享 Neo4j，通过 HTTP 内部通信。

```
┌──────────────────────────────────────────────────────────────┐
│                        前端（Vue 3）                          │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌───────────────┐  │
│  │Element    │ │AntV G6   │ │ECharts    │ │Pinia + Vue    │  │
│  │Plus       │ │(图谱可视化)│ │(统计图表)  │ │Router         │  │
│  └──────────┘ └──────────┘ └───────────┘ └───────────────┘  │
└──────────────────────┬───────────────────────────────────────┘
                       │ HTTP / SSE (对外)
┌──────────────────────┴───────────────────────────────────────┐
│              Java 主服务（Spring Boot 3.5 · :8888）            │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              API 层（Controller · 对前端）                │ │
│  │  用户/角色 │ 图谱项目 │ 图谱模型 │ 知识抽取 │ 图谱探索     │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │              Service 层（业务逻辑）                       │ │
│  │  用户服务 │ 项目服务 │ 模型服务 │ 转化服务 │ 图谱读写服务  │ │
│  │  标注服务 │ 抽取调度(调Python)                          │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │              数据访问层（Mapper / Repository）            │ │
│  │  MyBatis-Plus (MySQL)  │  Spring Data Neo4j (Neo4j)     │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │              基础设施层                                   │ │
│  │  Redis Session │ Knife4j │ Hutool │ AOP │ HttpClient    │ │
│  └─────────────────────────────────────────────────────────┘ │
└───────────┬──────────────────┬──────────────────┬───────────┘
            │                  │ HTTP             │
            │                  │ (内部调用)        │
            │         ┌────────┴───────────────────┐
            │         │  Python 微服务（FastAPI · :8001）│
            │         │  ┌────────────────────────┐  │
            │         │  │ LLM 知识抽取（本体驱动）│  │
            │         │  │  · Prompt 工程         │  │
            │         │  │  · 实体/关系抽取       │  │
            │         │  │  · 本体对齐            │  │
            │         │  ├────────────────────────┤  │
            │         │  │ LightRAG 开放式抽取    │  │
            │         │  │  · schema-free 模式   │  │
            │         │  │  · 多粒度检索         │  │
            │         │  ├────────────────────────┤  │
            │         │  │ 实体消歧与合并        │  │
            │         │  │ LLM 辅助本体生成      │  │
            │         │  └──────────┬─────────────┘  │
            │         │             │ Neo4j Driver    │
            │         └─────────────┼────────────────┘
            │                       │
     ┌──────┴──────┐    ┌──────────┴──────────┐
     │   MySQL     │    │       Neo4j          │    ┌─────────────┐
     │  (业务数据)  │    │     (图谱数据)        │    │  LLM API    │
     │             │    │  Java: 读写/查询      │    │ (DeepSeek)  │
     │ · 用户/角色  │    │  Python: 抽取写入     │    │             │
     │ · 图谱项目   │    │  · 实体节点           │    │ · 知识抽取   │
     │ · 本体模型   │    │  · 关系边             │    │ · 本体推荐   │
     │ · 语料/标注  │    │  · 节点属性           │    │ · 实体消歧   │
     │ · 抽取记录   │    │                      │    │             │
     │ · Prompt模板 │    │                      │    │             │
     └─────────────┘    └──────────────────────┘    └─────────────┘
```

**架构要点**：
- 前端只与 Java 交互，Python 对前端透明
- Java 通过 HTTP 调用 Python 的抽取接口，Python 抽取后直接写入 Neo4j
- Neo4j 两端共享：Java 用 Spring Data Neo4j 做查询展示，Python 用 neo4j 官方 Driver 做抽取写入
- 两种抽取模式并存：**本体驱动抽取**（默认，按预定义 Schema）+ **开放式抽取**（LightRAG，schema-free）

### 4.2 技术选型

#### 4.2.1 Java 主服务技术栈

| 技术 | 版本 | 用途 | 状态 |
|------|------|------|------|
| Spring Boot | 3.5.15 | 应用框架 | ✅ 已有 |
| Java | 21 | 编程语言 | ✅ 已有 |
| MyBatis-Plus | 3.5.15 | MySQL ORM | ✅ 已有 |
| Spring Data Neo4j | 7.x | Neo4j ORM（图谱读写/查询） | ✅ 已有 |
| Spring WebFlux | — | SSE 流式代理（智能问答透传） | ✅ 已有 |
| Spring Async | — | `@Async` 异步任务（MinerU 解析） | ✅ 已用 |
| Spring WebClient | — | 响应式 HTTP 客户端（调用 MinerU API） | ✅ 已用 |
| MinIO SDK | 8.x | 对象存储（语料文件存储） | ✅ 已用 |
| MinerU | v3.4.5 | 文档解析服务（PDF/Word → Markdown） | ✅ 已集成 |
| Spring Session + Redis | — | 会话管理 | ✅ 已有 |
| Knife4j | 4.4.0 | API 文档 | ✅ 已有 |
| Hutool | 5.8.38 | 工具类库（含 HttpClient 调 Python） | ✅ 已有 |
| Lombok | — | 代码简化 | ✅ 已有 |

#### 4.2.2 Python 微服务技术栈

| 技术 | 版本 | 用途 | 状态 |
|------|------|------|------|
| Python | 3.11+ | 编程语言 | ✅ 已用 |
| FastAPI | 0.115+ | Web 框架（异步高性能） | ✅ 已用 |
| Uvicorn | 0.30+ | ASGI 服务器 | ✅ 已用 |
| neo4j (官方 Driver) | 5.x | Neo4j Python 驱动（抽取写入 + Agent 工具查询） | ✅ 已用 |
| openai SDK | 1.x | DeepSeek API 调用（兼容 OpenAI 协议） | ✅ 已用 |
| LangChain / LangGraph | latest | Agent 编排 + 工具调用生命周期事件 | ✅ 已用 |
| langchain-openai | latest | ChatOpenAI 流式 | ✅ 已用 |
| Pydantic | 2.x | 数据校验与模型定义 | ✅ 已用 |
| PyMuPDF | 1.x | PDF 语料解析（text_processor） | ✅ 已用 |
| python-docx | 1.x | DOCX 语料解析（text_processor） | ✅ 已用 |

> **说明**：Spring AI 从 Java 技术栈中移除。LLM 调用全部由 Python 微服务承担，Java 仅通过 HttpClient 转发请求并接收 JSON 结果。

#### 4.2.3 前端技术栈

| 技术 | 用途 |
|------|------|
| Vue 3 + Vite | 前端框架与构建工具 |
| Element Plus | UI 组件库（管理后台界面） |
| AntV G6 | 图可视化引擎（图谱探索） |
| ECharts | 统计图表（首页仪表盘、训练效果） |
| Pinia | 状态管理 |
| Vue Router | 路由管理 |
| Axios | HTTP 请求 |
| @vueup/vue-quill | 富文本编辑器（Prompt 模板） |

#### 4.2.4 数据库选型

| 数据库 | 用途 | 访问端 | 核心表/节点 |
|--------|------|--------|------------|
| MySQL | 业务数据存储 | Java 独占 | user、role、graph_project、graph_model、graph_entity_type、graph_entity_property、graph_relation_type、graph_relation_property、corpus、extraction_task、prompt_template 等 |
| Neo4j | 图谱实例数据 | Java + Python 共享 | 节点（Entity）+ 关系（Relation），按 modelId 属性隔离 |
| Redis | 会话缓存 | Java 独占 | Spring Session 存储 |

> **Neo4j 双端访问分工**：Java（Spring Data Neo4j）负责图谱查询、可视化数据供给、结构化转化写入；Python（neo4j Driver）负责 LLM 抽取结果的批量写入与实体消歧操作。两端使用相同的 Cypher 参数化语句，避免注入。

### 4.3 后端工程结构

```
src/main/java/com/yuan/seedboot/
├── SeedBootApplication.java          # 启动类
├── common/                           # 通用类
│   ├── BaseResponse.java             # 统一响应
│   ├── PageRequest.java              # 分页请求
│   ├── ResultUtils.java              # 响应工具
│   └── DeleteRequest.java            # 删除请求
├── config/                           # 配置类
│   ├── CorsConfig.java               # 跨域配置
│   ├── Neo4jConfig.java              # Neo4j 配置 🆕
│   ├── PythonServiceConfig.java      # Python微服务地址配置 🆕
│   └── WebMvcConfig.java             # MVC 配置 🆕
├── controller/                       # 控制器
│   ├── ChatController.java           # 对话（已有）
│   ├── UserController.java           # 用户管理 🆕
│   ├── RoleController.java           # 角色管理 🆕
│   ├── GraphProjectController.java   # 图谱项目 🆕
│   ├── GraphModelController.java     # 图谱模型 🆕
│   ├── CorpusController.java         # 语料管理 🆕
│   ├── AnnotationController.java     # 数据标注 🆕
│   ├── ExtractionController.java     # 知识抽取 🆕
│   ├── LlmExtractionController.java  # LLM抽取 🆕
│   ├── StructureTransformController.java # 结构化转化 🆕
│   └── GraphExploreController.java   # 图谱探索 🆕
├── service/                          # 服务接口
│   ├── Impl/                         # 服务实现
│   ├── UserService.java              # （已有）
│   ├── GraphProjectService.java      # 🆕
│   ├── GraphModelService.java        # 🆕
│   ├── EntityTypeService.java        # 🆕 实体类型+属性CRUD
│   ├── RelationTypeService.java      # 🆕 关系类型+属性CRUD
│   ├── CorpusService.java            # 🆕
│   ├── AnnotationService.java        # 🆕
│   ├── ExtractionDispatchService.java# 🆕 调度Python抽取
│   ├── StructureTransformService.java# 🆕
│   ├── GraphExploreService.java      # 🆕
│   ├── PromptTemplateService.java    # 🆕
│   └── Neo4jService.java             # 🆕 Neo4j查询
├── mapper/                           # MyBatis Mapper
│   ├── UserMapper.java               # （已有）
│   ├── GraphProjectMapper.java       # 🆕
│   ├── GraphModelMapper.java         # 🆕
│   ├── GraphEntityTypeMapper.java    # 🆕
│   ├── GraphEntityPropertyMapper.java# 🆕
│   ├── GraphRelationTypeMapper.java  # 🆕
│   ├── GraphRelationPropertyMapper.java # 🆕
│   ├── CorpusMapper.java             # 🆕
│   ├── AnnotationMapper.java         # 🆕
│   ├── ExtractionTaskMapper.java     # 🆕
│   └── PromptTemplateMapper.java     # 🆕
├── model/
│   ├── entity/                       # 数据库实体
│   │   ├── User.java                 # （已有）
│   │   ├── Role.java                 # 🆕
│   │   ├── GraphProject.java         # 🆕
│   │   ├── GraphModel.java           # 🆕
│   │   ├── GraphEntityType.java     # 🆕
│   │   ├── GraphEntityProperty.java # 🆕
│   │   ├── GraphRelationType.java   # 🆕
│   │   ├── GraphRelationProperty.java # 🆕
│   │   ├── Corpus.java               # 🆕
│   │   ├── Annotation.java           # 🆕
│   │   ├── ExtractionTask.java       # 🆕
│   │   └── PromptTemplate.java       # 🆕
│   ├── dto/                          # 数据传输对象
│   │   ├── GraphProjectDTO.java
│   │   ├── LlmExtractionRequest.java
│   │   ├── GraphQueryRequest.java
│   │   └── ...
│   └── vo/                           # 视图对象
│       ├── GraphProjectVO.java
│       ├── GraphExploreVO.java
│       ├── ExtractionResultVO.java
│       └── ...
├── exception/                        # 异常处理
│   ├── BusinessException.java
│   ├── ErrorCode.java
│   ├── GlobalExceptionHandler.java
│   └── ThrowUtils.java
└── utils/                            # 工具类 🆕
    ├── Neo4jUtils.java
    └── PythonHttpClient.java         # 调用Python微服务
```

### 4.4 前端工程结构

```
kgraph-frontend/
├── public/
├── src/
│   ├── api/                          # API 请求
│   │   ├── request.ts                # Axios 封装
│   │   ├── user.ts
│   │   ├── graph.ts
│   │   ├── extraction.ts
│   │   └── explore.ts
│   ├── assets/                       # 静态资源
│   ├── components/                   # 公共组件
│   │   ├── GraphCanvas.vue           # 图谱可视化画布
│   │   ├── EntityDetailPanel.vue     # 实体详情面板
│   │   ├── PromptEditor.vue          # Prompt 编辑器
│   │   └── ...
│   ├── layout/                       # 布局组件
│   │   ├── MainLayout.vue            # 主布局（侧边栏+顶栏）
│   │   └── Sidebar.vue
│   ├── views/                        # 页面
│   │   ├── home/                     # 系统首页
│   │   ├── platform/                 # 平台管理
│   │   │   ├── UserManage.vue
│   │   │   ├── RoleManage.vue
│   │   │   └── ChangePassword.vue
│   │   ├── project/                  # 图谱项目管理
│   │   │   ├── GraphProjectList.vue
│   │   │   └── GraphModelManage.vue
│   │   ├── extraction/               # 知识抽取
│   │   │   ├── StructureTransform.vue
│   │   │   ├── CorpusManage.vue
│   │   │   ├── AnnotationAssign.vue
│   │   │   ├── DataAnnotation.vue
│   │   │   ├── AnnotationManage.vue
│   │   │   ├── ModelTrain.vue
│   │   │   ├── ModelEffect.vue
│   │   │   ├── DlExtraction.vue
│   │   │   └── LlmExtraction.vue
│   │   └── explore/                  # 图谱探索
│   │       └── GraphExplore.vue
│   ├── router/                       # 路由
│   │   └── index.ts
│   ├── store/                        # Pinia 状态
│   │   ├── user.ts
│   │   └── graph.ts
│   ├── utils/                        # 工具函数
│   ├── App.vue
│   └── main.ts
├── package.json
├── vite.config.ts
└── tsconfig.json
```

### 4.5 Python 微服务设计

#### 4.5.1 工程结构

```
kgraph-python/
├── app/
│   ├── main.py                       # FastAPI 入口
│   ├── config.py                     # 配置（LLM/Neo4j/端口）
│   ├── api/                          # API 路由
│   │   ├── extraction.py             # 知识抽取接口
│   │   ├── ontology.py               # LLM辅助本体生成
│   │   └── entity_resolution.py      # 实体消歧接口
│   ├── core/                         # 核心业务逻辑
│   │   ├── extractor.py              # 本体驱动抽取器
│   │   ├── lightrag_extractor.py     # LightRAG 开放式抽取器
│   │   ├── prompt_builder.py         # Prompt 工程构建器
│   │   ├── entity_resolver.py        # 实体消歧与合并
│   │   └── ontology_generator.py     # 本体自动生成
│   ├── models/                       # Pydantic 数据模型
│   │   ├── extraction.py             # 抽取请求/响应模型
│   │   ├── ontology.py               # 本体模型
│   │   └── graph.py                  # 图谱节点/关系模型
│   ├── graph/                        # Neo4j 操作层
│   │   ├── neo4j_client.py           # Neo4j 连接管理
│   │   └── graph_writer.py           # Cypher 批量写入
│   └── prompts/                      # Prompt 模板
│       ├── extraction_zh.txt         # 中文抽取 Prompt
│       ├── extraction_en.txt         # 英文抽取 Prompt
│       ├── ontology_gen.txt          # 本体生成 Prompt
│       └── entity_resolve.txt        # 实体消歧 Prompt
├── requirements.txt
├── .env
└── README.md
```

#### 4.5.2 核心模块说明

| 模块 | 职责 | 关键依赖 |
|------|------|----------|
| `extractor.py` | 本体驱动抽取：加载本体Schema → 构建 Prompt → 调用 LLM → 解析 JSON 结果 → 本体类型校验与对齐 | openai SDK |
| `lightrag_extractor.py` | 开放式抽取：调用 LightRAG 对文档做 schema-free 抽取，返回实体/关系/社区摘要 | LightRAG |
| `prompt_builder.py` | Prompt 模板渲染：变量替换（{text}、{ontology}、{examples}），支持 Zero-shot / Few-shot 模式 | jinja2 |
| `entity_resolver.py` | 实体消歧：查找同名/相似实体 → LLM 判断是否同一 → 合并 Cypher 执行 | openai SDK, neo4j |
| `ontology_generator.py` | 本体生成：输入领域描述 → LLM 推荐实体类型与关系类型 → 返回本体 JSON | openai SDK |
| `graph_writer.py` | Cypher 批量写入：UNWIND 参数化写入节点和关系，支持 MERGE 幂等 | neo4j |

#### 4.5.3 Python 微服务 API（内部接口）

> 以下接口仅供 Java 主服务内部调用，不直接暴露给前端。统一响应格式：`{"code": 0, "data": {}, "message": "ok"}`

| 接口 | 方法 | 路径 | 请求体 | 说明 |
|------|------|------|--------|------|
| 本体驱动抽取 | POST | /api/extract | `{text, ontology, modelId, mode, examples?}` | 抽取并直接写入 Neo4j，返回 `{entities:[], relations:[]}` |
| 开放式抽取 | POST | /api/extract/open | `{text, projectId?}` | LightRAG 模式，schema-free |
| 实体消歧 | POST | /api/entity/resolve | `{modelId, entityName}` | 查找相似实体并合并 |
| 本体生成 | POST | /api/ontology/generate | `{domain_description}` | LLM 推荐本体 JSON |
| 健康检查 | GET | /health | — | 服务存活探测 |

**抽取请求/响应示例**：

```json
// 请求 POST /api/extract
{
  "text": "张三出生于北京，毕业于清华大学，目前在阿里巴巴担任技术总监。",
  "ontology": {
    "entities": [
      {"name": "人物", "properties": [{"name": "姓名", "type": "string"}]},
      {"name": "地点", "properties": []},
      {"name": "组织", "properties": []}
    ],
    "relations": [
      {"name": "出生于", "sourceEntity": "人物", "targetEntity": "地点"},
      {"name": "毕业于", "sourceEntity": "人物", "targetEntity": "组织"},
      {"name": "就职于", "sourceEntity": "人物", "targetEntity": "组织"}
    ]
  },
  "mode": "zero_shot"
}

// 响应
{
  "code": 0,
  "data": {
    "entities": [
      {"name": "张三", "type": "人物", "properties": {"姓名": "张三"}, "start": 0, "end": 2},
      {"name": "北京", "type": "地点", "properties": {}, "start": 6, "end": 8},
      {"name": "清华大学", "type": "组织", "properties": {}, "start": 13, "end": 17},
      {"name": "阿里巴巴", "type": "组织", "properties": {}, "start": 21, "end": 25}
    ],
    "relations": [
      {"head": "张三", "relation": "出生于", "tail": "北京"},
      {"head": "张三", "relation": "毕业于", "tail": "清华大学"},
      {"head": "张三", "relation": "就职于", "tail": "阿里巴巴"}
    ]
  },
  "message": "ok"
}
```

#### 4.5.4 Prompt 工程设计

**本体驱动抽取 Prompt 模板**（`extraction_zh.txt`）：

```
你是一个知识图谱信息抽取专家。请从以下文本中抽取实体和关系，严格按照给定的本体模型。

## 本体模型
实体类型：
{ontology_entities}

关系类型：
{ontology_relations}

## 抽取规则
1. 实体名称必须来自原文，不可编造
2. 实体类型必须从本体定义的类型中选择
3. 关系的头实体和尾实体必须已在实体列表中
4. 关系类型必须从本体定义的关系类型中选择
5. 如果文本中无法抽取到符合本体的信息，返回空列表

## 输出格式（严格JSON，不要输出其他内容）
{{
  "entities": [
    {{"name": "实体名称", "type": "实体类型", "properties": {{}}, "start": 起始位置, "end": 结束位置}}
  ],
  "relations": [
    {{"head": "头实体名称", "relation": "关系类型", "tail": "尾实体名称"}}
  ]
}}

## Few-shot 示例（仅 few_shot 模式）
{examples}

## 待抽取文本
{text}
```


#### 4.5.5 LightRAG 集成方案

LightRAG 作为 **开放式抽取** 可选模式，与本体检索抽取并列：

| 对比项 | 本体驱动抽取（默认） | LightRAG 开放式抽取 |
|--------|---------------------|---------------------|
| Schema | 需预定义本体 | Schema-free，LLM 自主决定 |
| 适用场景 | 领域知识图谱（有明确Schema） | 探索性抽取（不知道抽什么） |
| 结果一致性 | 高（类型受约束） | 中（LLM 可能每次不同） |
| 人工审核 | 不需要（直接写入） | 可选（结果较粗放） |

**集成方式**：
- `lightrag_extractor.py` 封装 LightRAG 的 `query()` 和 `insert()`
- 开放式抽取结果直接写入 Neo4j（带 modelId 隔离）

---

## 五、数据库设计

### 5.1 MySQL 表设计

#### 5.1.1 用户与权限

```sql
-- 用户表（已有，扩展）
-- user: id, userAccount, userPassword, userName, userAvatar,
--        userProfile, userRole, editTime, createTime, updateTime, isDelete

-- 角色表
CREATE TABLE role (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    roleCode    VARCHAR(64) NOT NULL COMMENT '角色编码',
    roleName    VARCHAR(128) NOT NULL COMMENT '角色名称',
    description VARCHAR(512) COMMENT '描述',
    createTime  DATETIME DEFAULT CURRENT_TIMESTAMP,
    updateTime  DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    isDelete    TINYINT DEFAULT 0,
    UNIQUE KEY uk_roleCode (roleCode)
) COMMENT '角色';

-- 用户-角色关联表
CREATE TABLE user_role (
    id      BIGINT AUTO_INCREMENT PRIMARY KEY,
    userId  BIGINT NOT NULL,
    roleId  BIGINT NOT NULL,
    UNIQUE KEY uk_user_role (userId, roleId)
) COMMENT '用户角色关联';
```

#### 5.1.2 图谱项目与模型

**表关系**：graph_project (1) ── (N) graph_model ── (1:N) graph_entity_type / graph_relation_type

```sql
-- 图谱项目表
CREATE TABLE graph_project (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    projectName             VARCHAR(128) NOT NULL COMMENT '项目名称',
    projectDescription      TEXT COMMENT '项目描述',
    storageEngine           VARCHAR(32) DEFAULT 'neo4j' COMMENT '存储引擎类型',
    isConfiguredStorage     TINYINT(1) DEFAULT 0 COMMENT '是否已配置存储引擎: 0-否 1-是',
    isGraphSpaceCreated     TINYINT(1) DEFAULT 0 COMMENT '是否已创建图空间: 0-否 1-是',
    createBy                BIGINT COMMENT '创建人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除'
) COMMENT '图谱项目表';

-- 图谱模型表（本体元数据）
CREATE TABLE graph_model (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    projectId               BIGINT NOT NULL COMMENT '所属项目ID',
    modelName               VARCHAR(128) NOT NULL COMMENT '模型名称',
    modelDescription        TEXT COMMENT '模型描述',
    version                 INT DEFAULT 1 COMMENT '版本号',
    entityCount             INT DEFAULT 0 COMMENT '实体类型数量（缓存统计）',
    relationCount           INT DEFAULT 0 COMMENT '关系类型数量（缓存统计）',
    createBy                BIGINT COMMENT '创建人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_projectId (projectId)
) COMMENT '图谱模型（本体）表';

-- 实体类型表
CREATE TABLE graph_entity_type (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    modelId                 BIGINT NOT NULL COMMENT '关联模型ID',
    entityName              VARCHAR(128) NOT NULL COMMENT '实体类型名称',
    description             VARCHAR(512) COMMENT '实体描述',
    color                   VARCHAR(32) COMMENT '显示颜色',
    icon                    VARCHAR(64) COMMENT '图标',
    sortOrder               INT DEFAULT 0 COMMENT '排序',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_modelId (modelId)
) COMMENT '实体类型表';

-- 实体属性表
CREATE TABLE graph_entity_property (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    entityTypeId            BIGINT NOT NULL COMMENT '关联实体类型ID',
    propertyName            VARCHAR(128) NOT NULL COMMENT '属性名称',
    propertyType            VARCHAR(32) NOT NULL COMMENT '属性类型: string/number/date/boolean',
    isRequired              TINYINT(1) DEFAULT 0 COMMENT '是否必填: 0-否 1-是',
    defaultValue            VARCHAR(256) COMMENT '默认值',
    sortOrder               INT DEFAULT 0 COMMENT '排序',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_entityTypeId (entityTypeId)
) COMMENT '实体属性表';

-- 关系类型表
CREATE TABLE graph_relation_type (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    modelId                 BIGINT NOT NULL COMMENT '关联模型ID',
    relationName            VARCHAR(128) NOT NULL COMMENT '关系名称',
    description             VARCHAR(512) COMMENT '关系描述',
    sourceEntityTypeId      BIGINT NOT NULL COMMENT '起始实体类型ID',
    targetEntityTypeId      BIGINT NOT NULL COMMENT '终止实体类型ID',
    sortOrder               INT DEFAULT 0 COMMENT '排序',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_modelId (modelId),
    INDEX idx_sourceEntity (sourceEntityTypeId),
    INDEX idx_targetEntity (targetEntityTypeId)
) COMMENT '关系类型表';

-- 关系属性表
CREATE TABLE graph_relation_property (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    relationTypeId          BIGINT NOT NULL COMMENT '关联关系类型ID',
    propertyName            VARCHAR(128) NOT NULL COMMENT '属性名称',
    propertyType            VARCHAR(32) NOT NULL COMMENT '属性类型: string/number/date/boolean',
    isRequired              TINYINT(1) DEFAULT 0 COMMENT '是否必填: 0-否 1-是',
    defaultValue            VARCHAR(256) COMMENT '默认值',
    sortOrder               INT DEFAULT 0 COMMENT '排序',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_relationTypeId (relationTypeId)
) COMMENT '关系属性表';
```

**表关系图**：
```
graph_project (1) ── (N) graph_model
                         │
                         ├── (1:N) graph_entity_type ── (1:N) graph_entity_property
                         └── (1:N) graph_relation_type ── (1:N) graph_relation_property
                                      │ sourceEntityTypeId ──> graph_entity_type.id
                                      │ targetEntityTypeId ──> graph_entity_type.id
                                      └── (1:N) graph_relation_property
```

#### 5.1.3 语料管理

```sql
-- 语料表（已实现，集成 MinerU 文档解析）
CREATE TABLE corpus (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    projectId               BIGINT NOT NULL COMMENT '所属项目ID',
    title                   VARCHAR(256) COMMENT '语料标题',
    content                 MEDIUMTEXT NOT NULL COMMENT '语料文本内容（文本输入为原文；文件上传为 MinerU 解析后的 Markdown）',
    source                  VARCHAR(128) COMMENT '来源: manual(文本输入)/file(文档上传)',
    filePath                VARCHAR(512) COMMENT '文件路径（MinIO URL，仅 source=file 时有值）',
    fileType                VARCHAR(32) COMMENT '文件类型: pdf/docx（仅文档上传时有值）',
    mineruTaskId            VARCHAR(128) COMMENT 'MinerU 任务ID（预留字段，当前同步流程未填充）',
    errorMsg                VARCHAR(512) COMMENT '解析失败原因（成功时为 null）',
    status                  TINYINT DEFAULT 0 COMMENT '0-处理中 1-已完成 2-失败',
    createBy                BIGINT COMMENT '上传人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_projectId (projectId),
    INDEX idx_status (status)
) COMMENT '语料表';
```

> **数据迁移说明**：从旧版本升级时，原有文本语料需统一执行 `UPDATE corpus SET source='manual', status=1 WHERE source IS NULL OR source NOT IN ('manual','file');` 确保兼容新逻辑。

> **规划中表**：`corpus_chunk`（语料切片表，见 2.4.5 节），在文本切分功能落地时新增。

> 标注相关表（annotation_task、annotation）已实现，用于深度学习抽取的数据标注模块。

#### 5.1.4 知识抽取

```sql
-- 抽取任务表
CREATE TABLE extraction_task (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    projectId               BIGINT NOT NULL COMMENT '项目ID',
    modelId                 BIGINT NOT NULL COMMENT '图谱模型ID（用哪个本体抽）',
    corpusId                BIGINT COMMENT '语料ID（从哪段文本抽），NULL表示手动输入',
    inputText               MEDIUMTEXT COMMENT '手动输入文本（corpusId为空时使用）',
    extractionType          VARCHAR(32) DEFAULT 'LLM' COMMENT '抽取方式: LLM/DL/KOS/STRUCTURE',
    inputConfig             TEXT COMMENT '抽取配置JSON（模式、Prompt模板ID等）',
    result                  MEDIUMTEXT COMMENT '抽取结果JSON: {entities:[], relations:[]}',
    status                  TINYINT DEFAULT 0 COMMENT '0-排队 1-进行中 2-完成 3-失败',
    tokenConsumed           INT DEFAULT 0 COMMENT 'Token消耗',
    duration                BIGINT DEFAULT 0 COMMENT '耗时(毫秒)',
    createBy                BIGINT COMMENT '创建人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_projectId (projectId),
    INDEX idx_modelId (modelId),
    INDEX idx_corpusId (corpusId),
    INDEX idx_status (status)
) COMMENT '抽取任务表';

-- Prompt 模板表
CREATE TABLE prompt_template (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    templateName            VARCHAR(128) NOT NULL COMMENT '模板名称',
    category                VARCHAR(64) COMMENT '分类: EXTRACTION/ONTOLOGY/ENTITY_RESOLUTION',
    content                 TEXT NOT NULL COMMENT 'Prompt内容（含变量占位符）',
    variables               VARCHAR(512) COMMENT '变量列表JSON',
    description             VARCHAR(512) COMMENT '描述',
    isPublic                TINYINT(1) DEFAULT 1 COMMENT '是否公开: 0-否 1-是',
    createBy                BIGINT COMMENT '创建人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除'
) COMMENT 'Prompt模板表';
```

**抽取任务与模型/语料的绑定关系**：
```
extraction_task
    ├── modelId  ──> graph_model     (用哪个本体Schema约束抽取)
    ├── corpusId ──> corpus          (从哪段文本抽取)
    └── 结果写入 Neo4j (节点/关系带 modelId 隔离)
```

#### 5.1.5 数据标注与模型训练（已实现）

```sql
-- 标注任务表（数据标注模块，单表+JSON存储实体/关系标注数据）
CREATE TABLE annotation_task (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    taskName                VARCHAR(128) NOT NULL COMMENT '标注任务名称',
    projectId               BIGINT NOT NULL COMMENT '所属项目ID',
    corpusId                BIGINT COMMENT '语料ID',
    corpusTitle             VARCHAR(256) COMMENT '语料标题',
    text                    MEDIUMTEXT COMMENT '标注原文',
    annotator               VARCHAR(64) COMMENT '标注员',
    reviewer                VARCHAR(64) COMMENT '审核员',
    totalSentences          INT DEFAULT 0 COMMENT '总句子数',
    annotatedSentences      INT DEFAULT 0 COMMENT '已标注句子数',
    entities                MEDIUMTEXT COMMENT '实体标注JSON: [{text,type,sentenceIdx,start,end}]',
    relations               MEDIUMTEXT COMMENT '关系标注JSON: [{head,headId,relation,tail,tailId}]',
    createBy                BIGINT COMMENT '创建人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_projectId (projectId),
    INDEX idx_corpusId (corpusId)
) COMMENT '标注任务表';

-- 训练任务表（模型训练模块，单表+JSON存储配置/指标/历史）
CREATE TABLE train_task (
    id                      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    taskName                VARCHAR(128) NOT NULL COMMENT '训练任务名称',
    projectId               BIGINT NOT NULL COMMENT '所属项目ID',
    annotationTaskId        BIGINT COMMENT '关联标注任务ID',
    dataset                 VARCHAR(256) COMMENT '训练数据集名称',
    architecture            VARCHAR(64) NOT NULL COMMENT '模型架构: BiLSTM-CRF/BERT-CRF/SPAN-BERT/BERT-RE',
    version                 VARCHAR(32) COMMENT '版本号',
    status                  VARCHAR(32) DEFAULT 'pending' COMMENT '训练状态: pending/training/done/failed',
    progress                INT DEFAULT 0 COMMENT '训练进度百分比',
    currentEpoch            INT DEFAULT 0 COMMENT '当前轮次',
    epochs                  INT DEFAULT 20 COMMENT '总轮次',
    config                  TEXT COMMENT '训练配置JSON',
    metrics                 TEXT COMMENT '训练指标JSON: {loss,precision,recall,f1}',
    history                 MEDIUMTEXT COMMENT '训练历史JSON: [{epoch,loss,precision,recall,f1}]',
    createBy                BIGINT COMMENT '创建人ID',
    createTime              DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime              DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    isDeleted               TINYINT(1) DEFAULT 0 COMMENT '逻辑删除: 0-未删除 1-已删除',
    INDEX idx_projectId (projectId),
    INDEX idx_annotationTaskId (annotationTaskId),
    INDEX idx_status (status)
) COMMENT '训练任务表';
```

> **说明**：深度学习抽取的模型训练为模拟实现（trainer.py 用数学公式生成 loss/P/R/F1 曲线，dl_extractor.py 用规则词典匹配代替 BiLSTM-CRF 推理），便于功能演示与端到端流程验证。

### 5.2 Neo4j 图模型设计

**隔离策略**：按 `modelId` 属性隔离，一个模型的图谱实例独立于其他模型。统一标签 `Entity`，通过属性区分类型和模型。关系统一使用 `:RELATED` 类型，业务关系名存储在 `relationType` 属性中（保留 `type` 字段以兼容历史逻辑）。

```
节点示例:
(:Entity {
  id: "uuid",
  name: "张三",
  type: "人物",
  modelId: 1,
  properties: {年龄: 30, 职业: "工程师"},
  createTime: "2026-08-07T10:00:00"
})

关系示例:
(:Entity)-[:RELATED {
  id: "uuid",
  type: "出生于",              // 保留字段，兼容历史逻辑
  relationType: "出生于",      // 推荐字段，存储业务关系名
  modelId: 1,
  properties: {},
  createTime: "2026-08-07T10:00:00"
}]->(:Entity)
```

**查询时按 modelId 过滤**：
```cypher
// 查询某模型下所有实体
MATCH (n:Entity) WHERE n.modelId = 1 RETURN n LIMIT 100

// 查询某模型下的关系
MATCH (a:Entity)-[r:RELATED]->(b:Entity)
WHERE r.modelId = 1
RETURN a, r, b LIMIT 50

// 按业务关系类型筛选（推荐使用 relationType 字段）
MATCH (a:Entity)-[r:RELATED {relationType: '出生于'}]->(b:Entity)
WHERE a.modelId = 1
RETURN a.name, b.name

// 写入时带 modelId
MERGE (n:Entity {name: '张三', type: '人物', modelId: 1})
SET n += {age: 30, occupation: '工程师'}
```

> **设计说明**：使用统一 `:RELATED` 关系类型而非业务关系名作为 Neo4j 关系类型，避免 Neo4j 不支持动态关系类型带来的 Cypher 语句硬编码问题。前端图谱展示时从 `relationType` 字段读取业务关系名。

---

## 六、核心流程设计

### 6.1 LLM 知识抽取流程（Java → Python 协作）

```
前端：选择图谱模型(modelId) + 选择语料(corpusId)或手动输入文本
    │
    ▼
┌──────────────────────────────┐
│ Java: 接收请求                │ ← LlmExtractionController
│ 1. 查 graph_entity_type +     │   从 MySQL 加载模型的本体Schema
│    graph_entity_property      │   (实体类型+属性)
│ 2. 查 graph_relation_type +   │   (关系类型+属性)
│    graph_relation_property    │
│ 3. 查 corpus.content          │   获取语料文本（如有corpusId）
│    组装 {text, ontology,      │
│           modelId, mode}      │
└──────────┬───────────────────┘
           │ HTTP POST /api/extract
           ▼
┌──────────────────────────────┐
│ Python: 知识抽取              │ ← extractor.py
│ 4. prompt_builder 渲染模板    │   变量: {ontology}, {text}, {examples}
│    (本体Schema + 语料文本)    │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Python: 调用 LLM API          │ ← openai SDK → DeepSeek
│ 5. 流式获取结果               │   要求输出严格 JSON
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Python: 解析与校验            │ ← extractor.py
│ 6. JSON解析                   │   实体类型校验(本体对齐)
│    类型校验 + 本体对齐        │   非法类型 → LLM推荐最近类型
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Python: 写入 Neo4j           │ ← graph_writer.py
│ 7. Cypher MERGE 批量写入      │   节点/关系带 modelId 隔离
│    返回写入结果               │   {entities:[], relations:[]} + 写入统计
└──────────┬───────────────────┘
           │ 返回 JSON
           ▼
┌──────────────────────────────┐
│ Java: 结果处理                │ ← ExtractionDispatchService
│ 8. 保存抽取记录到 MySQL       │   extraction_task (modelId, corpusId)
│    SSE 推送前端               │   Flux<String> 流式返回
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 前端: 结果展示                │
│ 9. 原文高亮标注               │   实体着色 + 结构化表格
│    写入统计                   │   实体数/关系数/耗时
└──────────────────────────────┘
```

**关键设计**：
- 抽取任务绑定 modelId（用哪个本体）+ corpusId（从哪段文本抽），两者缺一不可
- Python 抽取后直接写入 Neo4j（带 modelId 隔离），无需人工审核
- Neo4j 节点和关系都带 modelId 属性，实现不同模型的图谱实例隔离
- Java 通过 SSE 将 Python 的流式结果转发给前端，前端实时看到抽取进度
- 抽取模式可选：`zero_shot`（无示例）/ `few_shot`（带示例）/ `open`（LightRAG 开放式）

### 6.2 结构化数据转化流程

```
上传 CSV/Excel
    │
    ▼
解析文件 → 提取列名与示例数据
    │
    ▼
列映射配置：数据列 → 实体属性
    │
    ▼
指定实体标识列 + 关系映射规则
    │
    ▼
批量生成 Cypher CREATE 语句 → 写入 Neo4j
    │
    ▼
转化结果统计报告
```

### 6.3 图谱探索流程

```
选择图谱项目
    │
    ▼
加载初始节点（默认展示前100个实体）
    │
    ▼
AntV G6 力导向布局渲染
    │
    ▼
用户交互:
├── 点击节点 → 查询邻居 → 展开子图
├── 搜索实体 → 高亮定位
├── 类型筛选 → 过滤显示
├── 路径分析 → Cypher最短路径查询
└── 导出 → PNG/JSON
```

---

## 七、API 设计规范

### 7.1 统一响应格式

```json
{
  "code": 0,
  "data": {},
  "message": "ok"
}
```

### 7.2 Java 对外 API（前端调用）

> 以下为 Java 主服务暴露给前端的 API，Python 微服务 API 见 4.5.3 节。

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| **用户** | POST | /user/login | 用户登录 |
| | POST | /user/register | 用户注册 |
| | GET | /user/list | 用户列表（分页） |
| | POST | /user/add | 新增用户 |
| | PUT | /user/update | 编辑用户 |
| | DELETE | /user/delete/{id} | 删除用户 |
| | POST | /user/password | 修改密码 |
| **角色** | GET | /role/list | 角色列表 |
| | POST | /role/add | 新增角色 |
| | PUT | /role/update | 编辑角色 |
| | DELETE | /role/delete/{id} | 删除角色 |
| | POST | /role/assign | 分配用户角色 |
| **图谱项目** | GET | /graph/project/list | 项目列表 |
| | POST | /graph/project/add | 新建项目 |
| | PUT | /graph/project/update | 编辑项目 |
| | DELETE | /graph/project/delete/{id} | 删除项目 |
| | GET | /graph/project/stats/{id} | 项目统计 |
| **图谱模型** | GET | /graph/model/list/{projectId} | 项目下模型列表 |
| | POST | /graph/model/add | 新建模型 |
| | POST | /graph/model/llm-generate | LLM辅助生成模型 |
| | GET | /graph/model/{modelId} | 获取模型详情(含实体/关系类型) |
| | POST | /graph/entity-type/add | 新增实体类型 |
| | PUT | /graph/entity-type/update | 编辑实体类型 |
| | DELETE | /graph/entity-type/delete/{id} | 删除实体类型 |
| | POST | /graph/entity-property/add | 新增实体属性 |
| | DELETE | /graph/entity-property/delete/{id} | 删除实体属性 |
| | POST | /graph/relation-type/add | 新增关系类型 |
| | PUT | /graph/relation-type/update | 编辑关系类型 |
| | DELETE | /graph/relation-type/delete/{id} | 删除关系类型 |
| | POST | /graph/relation-property/add | 新增关系属性 |
| | DELETE | /graph/relation-property/delete/{id} | 删除关系属性 |
| **语料** | GET | /corpus/list | 语料列表（分页） |
| | POST | /corpus/add | 新增语料（文本输入） |
| | POST | /corpus/upload | 上传文档创建语料（PDF/Word，异步调用 MinerU 解析） |
| | POST | /corpus/reparse | 重新解析文档语料 |
| | POST | /corpus/update | 编辑语料 |
| | POST | /corpus/delete | 删除语料 |
| | GET | /corpus/get | 语料详情 |
| **标注** | GET | /annotation/tasks | 标注任务列表 |
| | POST | /annotation/assign | 分配标注任务 |
| | GET | /annotation/{taskId} | 获取标注数据 |
| | POST | /annotation/save | 保存标注结果 |
| | PUT | /annotation/review | 审核标注 |
| **LLM抽取** | POST | /extraction/llm | 执行LLM抽取（Java转发Python，SSE流式） |
| | GET | /extraction/history | 抽取历史 |

| **Prompt模板** | GET | /prompt/list | 模板列表 |
| | POST | /prompt/save | 保存模板 |
| | DELETE | /prompt/delete/{id} | 删除模板 |
| **结构化转化** | POST | /transform/upload | 上传结构化文件 |
| | POST | /transform/execute | 执行转化 |
| **图谱探索** | GET | /explore/nodes/{modelId} | 获取节点(按modelId隔离) |
| | GET | /explore/neighbors/{nodeId} | 获取邻居 |
| | POST | /explore/search | 搜索实体 |
| | POST | /explore/path | 路径分析 |
| | POST | /explore/cypher | Cypher查询 |
| | GET | /explore/stats/{modelId} | 图谱统计 |

---

## 八、MVP 范围与开发计划

### 8.1 MVP 核心闭环

```
用户登录 → 创建图谱项目 → 定义图谱模型 → LLM知识抽取 → 写入Neo4j → 图谱探索
```

### 8.2 优先级划分

| 优先级 | 模块 | 功能 | 说明 |
|--------|------|------|------|
| **P0** | 平台管理 | 登录/用户管理 | 基础认证（复用现有User表） |
| **P0** | 图谱项目管理 | 项目CRUD | 图谱空间隔离 |
| **P0** | 图谱模型管理 | 本体定义 | 实体/关系类型管理 |
| **P0** | LLM知识抽取 | 抽取+写入图谱 | 核心差异化功能 |
| **P0** | 图谱探索 | 可视化浏览 | AntV G6 图可视化 |
| **P1** | 系统首页 | 仪表盘 | 统计卡片+图表 |
| **P1** | 结构化转化 | CSV导入 | 数据快速入库 |
| **P1** | 角色管理 | RBAC | 基础角色权限 |
| **P2** | 语料管理 | 上传/列表 | 页面搭建 |
| **P2** | 数据标注 | 标注界面 | 页面搭建 |
| **P2** | 模型训练 | 训练管理 | Mock 流程 |
| **P3** | 标注授权 | 任务分配 | 页面搭建 |
| **P3** | 标注管理 | 审核导出 | 页面搭建 |
| **P3** | 模型效果 | 指标展示 | Mock 数据 |
| **P3** | DL知识抽取 | 传统抽取 | Mock 调用 |

### 8.3 三天开发计划

#### Day 1：基础框架 + 核心链路

- [ ] 后端(Java)：Neo4j 集成，Spring Data Neo4j 配置
- [ ] 后端(Java)：图谱项目 CRUD（MySQL）
- [ ] 后端(Java)：图谱模型管理（MySQL + JSON）
- [ ] 后端(Java)：Neo4j 写入服务（Cypher）+ PythonServiceConfig
- [ ] Python：FastAPI 项目初始化 + neo4j/openai 依赖安装
- [ ] Python：本体驱动抽取器（prompt_builder + extractor + /api/extract）
- [ ] 前端：项目初始化（Vue 3 + Vite + Element Plus）
- [ ] 前端：布局组件 + 路由 + 登录页

#### Day 2：前端核心功能 + 图谱探索 + Python 问答

- [ ] 前端：图谱项目管理页面
- [ ] 前端：图谱模型管理页面（本体编辑器）
- [ ] 前端：LLM 知识抽取页面（Prompt + 流式结果 + 审核）
- [ ] 前端：图谱探索页面（AntV G6 可视化）
- [ ] 后端(Java)：图谱探索 API（节点查询、搜索、路径分析）
- [ ] 后端(Java)：结构化数据转化 API
- [ ] Python：LLM 辅助本体生成（/api/ontology/generate）
- [ ] 联调：Java ↔ Python 抽取链路打通

#### Day 3：完善 + 非核心功能 + 增强

- [ ] 前端：系统首页仪表盘
- [ ] 前端：用户管理 + 角色管理
- [ ] 前端：结构化转化页面
- [ ] 前端：语料管理 + 标注页面（框架搭建）
- [ ] 前端：模型训练/效果页面（Mock 数据）
- [ ] 后端(Java)：Prompt 模板管理
- [ ] Python：LightRAG 开放式抽取集成（/api/extract/open）
- [ ] Python：实体消歧与合并（/api/entity/resolve）
- [ ] 联调测试 + UI 优化

---

## 九、关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| Neo4j 隔离方式 | 属性隔离（modelId 属性） | 一个项目多个模型，按 modelId 隔离图谱实例，查询时 WHERE n.modelId = ? |
| Neo4j 关系类型存储 | 统一 `:RELATED` 类型 + `relationType` 属性 | Neo4j 不支持动态关系类型，使用统一类型避免 Cypher 硬编码；业务关系名存于 `relationType` 属性，保留 `type` 字段兼容历史逻辑 |
| 项目与模型关系 | 1:N（一个项目多个模型） | 项目是空间容器，模型定义本体Schema，抽取时绑定具体模型 |
| 模型存储方式 | 拆表（entity_type/property + relation_type/property） | 需求要求实体/关系/属性独立CRUD，拆表比JSON字段更规范 |
| 语料与模型关系 | 正交独立，通过抽取任务绑定 | 模型定义"抽什么"，语料定义"从哪抽"，抽取任务组合两者 |
| 语料来源类型 | `source` 字段区分 `manual`/`file` | 文本输入与文档上传两种入口共用 corpus 表，文档上传走 MinerU 异步解析 |
| 文档解析方案 | MinerU v3.4.5 + 异步任务 | MinerU 开源稳定、支持 PDF/Word 输出 Markdown；`@Async` 避免阻塞上传响应，前端轮询感知完成 |
| 文档解析结果存储 | 整段 Markdown 存入 `corpus.content` | 简化实现，保留结构信息；后续文本切分将拆分为 `corpus_chunk` 表 |
| LLM 输出格式约束 | JSON Schema Prompt | 要求 LLM 输出严格 JSON，后端解析校验 |
| 图可视化引擎 | AntV G6 | 国产、文档完善、力导向布局性能好 |
| 图谱探索布局 | flex 三栏布局，详情面板挤压中间图谱 | 详情展开时挤压画布而非覆盖，关闭时 width=0 完全收回，G6 `changeSize` 同步画布尺寸 |
| 会话认证 | Spring Session + Redis | 已有基础，无需引入 JWT |
| 密码加密 | MD5 + 盐值 | 沿用现有 User 表方案 |
| 前端构建 | Vite | Vue 3 标准搭配，开发体验好 |
| 深度学习流水线 | Mock 实现 | 3 天时限内无法训练真实模型，页面完整 + Mock 数据 |
| 图谱写入 | Cypher 参数化查询（MERGE 幂等） | 防注入，重复抽取不会产生重复节点 |
| LLM 调用归属 | Python 微服务统一负责 | Python 生态 LLM 库丰富，Prompt 工程迭代快，与 Java 解耦 |
| Java↔Python 通信 | HTTP REST（非 gRPC/消息队列） | MVP 简单可靠，调试方便，后期可升级 gRPC |
| 抽取结果写入 | Python 抽取后直接写入 Neo4j | 简化流程，Python 负责抽取+写入，Java 只负责调度和记录 |
| Neo4j 双端访问 | Java 查询 + Python 写入分离 | Java 用 Spring Data Neo4j 做查询展示，Python 用 neo4j Driver 做抽取写入 |
| 智能问答 Agent | LangGraph + 6 个图谱工具 | Agent 自主选择工具查询图谱，`recursion_limit=50` + SYSTEM_PROMPT 限制 3 次工具调用，避免递归循环 |
| 智能问答流式传输 | Python SSE → Java WebFlux → 前端打字机 | 全链路增量推送，三路独立打字机（思考/工具/回答），参考 DeepSeek 视觉风格 |
| Spring AI 移除 | LLM 调用全部移至 Python | 避免 Java/Python 双端维护 LLM 配置，职责单一化 |

---

## 十、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| LLM 输出格式不稳定 | 抽取结果解析失败 | Prompt 严格约束 + JSON 容错解析 + 重试机制 |
| Neo4j 未安装 | 图谱功能不可用 | 提前安装 Docker Neo4j，编写初始化脚本 |
| MinerU 服务未启动 | 文档解析不可用 | 前端上传后异步轮询，失败可手动重新解析；MinerU 配置仅在 `application-local.yml` 中，新增环境时需手动补上 |
| MinerU 解析超时（>10 分钟） | 上传请求长时间无响应 | 已设置 10 分钟超时 + `@Async` 不阻塞上传接口 + 失败回写 `errorMsg` |
| 长 PDF 解析后文本超 LLM 上下文 | LLM 抽取失败或截断 | 规划中：MinerU 解析后文本切分（见 2.4.5），按 chunk 调用 LLM |
| MinerU 响应格式变更 | 解析失败 | `extractMarkdown` 已兼容多种格式（v3.4.5 / 旧版数组 / 纯文本兜底） |
| 智能问答 Agent 递归循环 | 触发 LangGraph 递归限制（25 次） | 已新增 `get_entities_by_type` 工具 + SYSTEM_PROMPT 限制 3 次调用 + `recursion_limit=50` 兜底 |
| 前端工作量大的页面 | 图谱探索、标注界面 | 使用组件库快速搭建，标注界面降级 |
| G6 学习成本 | 图谱探索延迟 | 使用 G6 基础 API，参考官方力导向示例 |
| Spring AI 流式响应 | 前端接收复杂 | 沿用现有 SSE 方案（ChatController 已验证） |
| Python 服务未启动 | 抽取/问答功能不可用 | Java 端做健康检查兜底，返回友好提示 |
| Java↔Python 跨服务调试 | 链路问题定位慢 | 统一日志格式 + 请求 traceId 贯穿两端 |
| Neo4j 双端访问 | Java 查询 + Python 写入 | 职责分离，Python 负责抽取写入，Java 负责查询展示 |
